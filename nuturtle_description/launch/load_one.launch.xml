<?xml version="1.0"?>
<launch>
  <arg name="use_rviz" default="true" 
    description="The user can control whether the turtlebot3 is shown in Rviz or not at launch."/>
  <arg name="use_jsp" default="true" 
    description="The joint state publisher can be true or false. If true, the joint state publisher will launch default joint states"/>

  <!-- Robot state publisher -->
  <node pkg="robot_state_publisher" exec="robot_state_publisher">
    <param name="robot_description" value="$(command '$(exec-in-pkg xacro xacro) $(find-pkg-share nuturtle_description)/urdf/turtlebot3_burger.urdf.xacro')"/>
  </node>

  <!-- Joint state publisher -->
  <node pkg="joint_state_publisher" exec="joint_state_publisher" if="$(eval '\'$(var use_jsp)\'==\'true\'')"/>

  <!-- TODO: Ask if this should be used if false in office-hours. -->
  <!-- Joint state publisher GUI -->
  <!-- <node pkg="joint_state_publisher_gui" exec="joint_state_publisher_gui" if="$(eval '\'$(var use_jsp)\'==\'false\'')"/> -->

  <!-- RViz - If RViz is launched and closed, the launchfile will terminate-->
  <node pkg="rviz2" exec="rviz2" args="-d $(find-pkg-share nuturtle_description)/config/load_one.rviz" on_exit="shutdown"/>
</launch>